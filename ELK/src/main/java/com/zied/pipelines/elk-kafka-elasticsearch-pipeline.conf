input {
    kafka{
        bootstrap_servers => "kafka:9092"
        topics => ["log-test"]
      }
}
# The filter part of this file is commented out to indicate that it is
# optional.
# filter {
#   Custom Transformations can be created here
# }
output {
    elasticsearch {
          hosts => ["elasticsearch:9200"]
          index => "kafka_test"
    }
}